import { ComparePage } from "@/components/collectors/compare-page";

export const metadata = { title: "Compare artwork | EduthArt" };

export default function CompareRoute() {
  return <ComparePage />;
}
